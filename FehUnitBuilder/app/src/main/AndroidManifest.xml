<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.example.fehunitbuilder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SacredSealSelectionActivity"></activity>
        <activity android:name=".CSkillSelectionActivity" />
        <activity android:name=".BSkillSelectionActivity" />
        <activity android:name=".ASkillSelectionActivity" />
        <activity android:name=".SpecialSelectionActivity" />
        <activity android:name=".AssistSelectionActivity" />
        <activity android:name=".WeaponSelectionActivity" />
        <activity android:name=".UnitSelectionActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewBuildActivity"
            android:label="@string/app_name" />
    </application>

</manifest>